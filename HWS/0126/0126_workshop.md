# 0126_workshop

## 1. pip

(1) 가짜 데이터를 만들기 위한 패키지 설치를 위해 사용하는 명령이다.

(2) git bash

## 2. Basic Usages

1. 원하는 데이터 형식의 이름을 따서 명명된 속성에 엑세스하여 데이터를 생성할 수 있는 페이커 생성기를 만들고 초기화를 하기 위한 코드이다.
2. 모듈, 파일명(라이브러리)
3. 메서드



## 3. Localization

(a) : `init`

(b) : `self`

(c) : `name`





## 4. Seeding the Generator

1. 이도윤
2. 이지후

파라미터와 반환값이 모두 존재하는 메소드



## 4. Seeding the Generator

1. 이도윤
2. 김현우

`seed()` : 동일한 버전의 페이커와 시드로 동일한 메서드를 호출하면 동일한 결과가 생성된다.

`seed_instance()` : 공유 된 인스턴스와 분리된 자체 인스턴스로 전환한 것.